package neetcode.dynamic_programming;

public class Maximal_Rectangle_LC_85 {
    class Solution {
        public int maximalRectangle(char[][] matrix) {
            if(matrix == null || matrix.length == 0) {
                return 0;
            }

            int ROWS = matrix.length, COLS = matrix[0].length;


            return -1;
        }
    }
}
